import ProjectTemplate from "../components/portfolio/ProjectTemplate";

const ProjectDetail = () => {
  window.scrollTo(0, 0);

  return (
    <section>
      <ProjectTemplate />
    </section>
  );
};

export default ProjectDetail;
